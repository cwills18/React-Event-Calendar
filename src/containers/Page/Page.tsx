import MonthlyView from "../MonthlyView/MonthlyView";

const Page = () => {
	// const loadTodaysDate = React.useContext(DatesContext);

	// useEffect(() => {
	//     loadTodaysDate();
	// }, [])

	return (
		<>
			<MonthlyView />
		</>
	);
};

export default Page;
